import styled from 'styled-components';

export const ValueContainer = styled.div({
  padding: 10,
  borderRadius: 5,
  borderWidth: 1,
  borderStyle: 'solid',
  borderColor: 'black',
  backgroundColor: 'white',
});

export const Container = styled.header({
  paddingBlock: 10,
  paddingInline: 20,
  background: 'linear-gradient(to right , #F88484, #F5F5F5)',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
});
